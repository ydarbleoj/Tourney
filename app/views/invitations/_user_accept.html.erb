<%= form_for [:accepted, @invitation], as: :user, url: accepted_tournamnet_invitation_path(@invitation.tournament.id, @invitation.token) do |f| %>
   <%= f.submit "Accept Invitation", class: 'button outline expanded'  %>
<% end %>